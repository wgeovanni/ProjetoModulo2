const path = require('path');

// Exporta as variáveis que receberão os caminhos de seus respectivos arquivos
module.exports = {
    "config": path.resolve(__dirname, "src", "config", "database_config.js"),
    "models_path": path.resolve(__dirname, "src", "models"),
    "migrations_path": path.resolve(__dirname, "src", "database", "migrations")
}